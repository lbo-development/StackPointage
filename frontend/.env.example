# ============================================================
# FRONTEND — Variables d'environnement
# Copier ce fichier en .env et remplir les valeurs
# ============================================================

# URL de l'API backend
# En dev : grâce au proxy Vite, on peut laisser vide (le proxy gère /api → localhost:3001)
# En production Railway : mettre l'URL complète du backend
VITE_API_URL=http://localhost:3001
